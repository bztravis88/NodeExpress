const sayHi = (name) => {
    console.log(`hello there ${name}`)  // string template literal (backticks)
    console.log(`2 + 2 = ${2 + 2}`)     // hey that's kinda cool
}

// sayHi("dude")

module.exports = sayHi
// console.log(module)